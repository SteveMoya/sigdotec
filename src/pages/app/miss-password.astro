---
export const prerender = false
import Layout from "@layouts/Layout.astro";
//! Se agrego un alias para diferencias, NOTA: Los componentes adentro de la carpeta 'pages' de astro tienen que ser nombrados en minusculas
import { MissPassword as Password } from '@components/App/MissPassword'
//!Solo los componentes se deben de nombrar en Capitalizate

---

<Layout>
    <Password client:load>
    </Password>
</Layout>
