export type User = {
    id: string
    userimage: string
    username: string
    createdAt: string
    email: string
    emailVerificated: boolean
    role: string
    provider: string
    balance: number
}